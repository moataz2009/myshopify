{%- comment -%}
链接按钮组件:
space_tight：文本与箭头间距true:紧密，默认为宽松
{%- endcomment -%}
{% assign buy_title = product.title %}
{% assign buy_url = product.url %}
<div class="top-buyBar px-3 sm:px-6 md:px-14 py-2 w-full h-12 flex items-center justify-between transition" id="buyBar">
  <p class="text-xs xs:text-base leading-4 xs:leading-8 font-bold max-15rem xs:max-w-full">
    {{ buy_title}}
  </p>
  <a href="{{buy_url}}" class="inline-block opacity-100">
    <button
      class="px-3 py-1 xl:px-5 text-xs sm:text-sm font-bold transform transition duration-300 ease-in-out bg-white text-black">
      Shop Now
    </button>
  </a>
</div>
<style>
  .top-buyBar{
    z-index: 10;
    overflow: hidden;
  }
  .top-buyBar::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    background-color: #000;
    width: 100%;
    height: 0;
    transition: all .3s ease-in-out;
    /* -webkit-filter: blur(5px);
    -moz-filter: blur(5px);
    -ms-filter: blur(5px);
    -o-filter: blur(5px);
    filter: blur(5px); */
    z-index: -1;
  }
  .top-buyBar.active::after{
    height: 100%;
  }
  @media screen and (max-width:576px) {
    .max-15rem {
      max-width: 14rem;
    }
  }
</style>

<script type="text/javascript">
  $(function () {
    var nav = $("#buyBar"); //得到导航对象  
    var win = $(window); //得到窗口对象  
    var sc = $(document); //得到document文档对象。  
    win.scroll(function () {
      if (sc.scrollTop() >= 72) {
        nav.addClass("active");
        nav.addClass("top-0");
        nav.addClass("fixed");
        nav.addClass("z-50");
        nav.removeClass("t-18");
      } else {
        nav.removeClass("top-0");
        nav.removeClass("fixed");
        nav.removeClass("z-50");
        nav.removeClass("active");
      }
    })
  })
</script>